apiVersion: v1
kind: Service
metadata:

  labels:
    App: frontend
  name: frontend
spec:
  ports:
    - name: "80"
      port: 80
      targetPort: 5000
  selector:
    App: frontend
status:
  loadBalancer: {}
